app:
  name: base-app
  # Placeholder referencing another key in this same file.
  greeting: "Hello, ${app.name}!"
  timeout: 30
  tags:
    - core
    - stable

server:
  port: 8080

logging:
  level:
    root: INFO

spring:
  datasource:
    url: jdbc:h2:mem:base
    username: sa
